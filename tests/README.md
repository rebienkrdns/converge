# Tests

The `tests/` directory contains conformance-oriented fixtures and examples.

These tests are documentation-first because Converge is a framework standard, not an executable library in this repository.

