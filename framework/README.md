# Framework

The `framework/` directory contains the operational model of Converge.

It defines how decisions are processed, who evaluates them, what outputs are produced, and which gates can block execution.

